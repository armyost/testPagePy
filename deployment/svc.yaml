apiVersion: v1
kind: Service
metadata:
  name: hellopython
  namespace: ns-jenkins
spec:
  type: NodePort
  selector:
    app: hellopython
  ports:
    - name: hellopython
      port: 8090
      targetPort: 80
